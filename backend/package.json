{
  "name": "utoy-shop-backend",
  "version": "1.0.0",
  "description": "API segura y persistencia de catálogo/pedidos para UTOY DROP",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check index.js && node --check src/app.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": { "node": ">=20" },
  "private": true
}
