{
  "name": "utoy-shop-backend",
  "version": "2.0.0",
  "description": "Cloudflare Worker de UTOY DROP con D1 y R2",
  "type": "module",
  "main": "src/worker.js",
  "scripts": {
    "test": "vitest run",
    "check": "node --check src/worker.js && node --check src/catalog.js && node --check src/orders.js && node --check src/security.js && node --check src/validation.js"
  },
  "dependencies": {
    "hono": "4.12.30"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.5",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": "22.x"
  },
  "private": true
}
